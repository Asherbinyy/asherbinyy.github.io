# Content provenance

The owner authorized using `docs/07-CONTENT-SCHEMA.md` for Milestone 1.
The JSON here transcribes its profile, career, application, and education
examples. Literal `...` translations and unspecified fields remain null or
absent. No missing role description, metric, application, or translation was
completed by inference.

`fallback.json` uses the same documented name, positioning and contact fields
as `profile.json`. It is the minimum identity/contact shape accepted by Profile.
The referenced PDF and portrait do not exist; their profile references are null
until the owner supplies them. The Calendly placeholder is also null.

Still missing: most Arabic copy, five full career descriptions, the twelfth
application, Mokaf store links, the PDF, images, and case study prose. Writing
and UI-string JSON structures are not defined in docs/07; those files have not
been invented. UI strings belong in ARB under the Flutter standards.
