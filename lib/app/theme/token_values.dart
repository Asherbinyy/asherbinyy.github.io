part of 'tokens.dart';

/// Makes all shared measurements available through context.tokens.
extension ThemeTokenValues on ThemeTokens {
  /// Settled state.
  Duration get noMotion => Tokens.noMotion;

  /// Shared zero token.
  double get zero => Tokens.zero;

  /// Shared grainOpacity token.
  double get grainOpacity => Tokens.grainOpacity;

  /// Shared amberCoverageLimit token.
  double get amberCoverageLimit => Tokens.amberCoverageLimit;

  /// Shared space4 token.
  double get space4 => Tokens.space4;

  /// Shared space8 token.
  double get space8 => Tokens.space8;

  /// Shared space12 token.
  double get space12 => Tokens.space12;

  /// Shared space16 token.
  double get space16 => Tokens.space16;

  /// Shared space24 token.
  double get space24 => Tokens.space24;

  /// Shared space32 token.
  double get space32 => Tokens.space32;

  /// Shared space48 token.
  double get space48 => Tokens.space48;

  /// Shared space64 token.
  double get space64 => Tokens.space64;

  /// Shared space96 token.
  double get space96 => Tokens.space96;

  /// Shared space128 token.
  double get space128 => Tokens.space128;

  /// Shared gridUnit token.
  double get gridUnit => Tokens.gridUnit;

  /// Shared mediumBreakpoint token.
  double get mediumBreakpoint => Tokens.mediumBreakpoint;

  /// Shared expandedBreakpoint token.
  double get expandedBreakpoint => Tokens.expandedBreakpoint;

  /// Shared largeBreakpoint token.
  double get largeBreakpoint => Tokens.largeBreakpoint;

  /// Shared compactGutter token.
  double get compactGutter => Tokens.compactGutter;

  /// Shared mediumGutter token.
  double get mediumGutter => Tokens.mediumGutter;

  /// Shared expandedGutter token.
  double get expandedGutter => Tokens.expandedGutter;

  /// Shared largeGutter token.
  double get largeGutter => Tokens.largeGutter;

  /// Shared expandedContentWidth token.
  double get expandedContentWidth => Tokens.expandedContentWidth;

  /// Shared largeContentWidth token.
  double get largeContentWidth => Tokens.largeContentWidth;

  /// Shared expandedColumns token.
  int get expandedColumns => Tokens.expandedColumns;

  /// Shared panelRadius token.
  double get panelRadius => Tokens.panelRadius;

  /// Shared tagRadius token.
  double get tagRadius => Tokens.tagRadius;

  /// Shared controlRadius token.
  double get controlRadius => Tokens.controlRadius;

  /// Shared modalRadius token.
  double get modalRadius => Tokens.modalRadius;

  /// Shared hairlineWidth token.
  double get hairlineWidth => Tokens.hairlineWidth;

  /// Shared cornerTickLength token.
  double get cornerTickLength => Tokens.cornerTickLength;

  /// Shared cornerTickOffset token.
  double get cornerTickOffset => Tokens.cornerTickOffset;

  /// Shared displayFamily token.
  String get displayFamily => Tokens.displayFamily;

  /// Shared bodyFamily token.
  String get bodyFamily => Tokens.bodyFamily;

  /// Shared arabicFamily token.
  String get arabicFamily => Tokens.arabicFamily;

  /// Shared telemetryFamily token.
  String get telemetryFamily => Tokens.telemetryFamily;

  /// Shared displayXlMin token.
  double get displayXlMin => Tokens.displayXlMin;

  /// Shared displayXlMax token.
  double get displayXlMax => Tokens.displayXlMax;

  /// Shared displayXlViewportFactor token.
  double get displayXlViewportFactor => Tokens.displayXlViewportFactor;

  /// Shared displayXlHeight token.
  double get displayXlHeight => Tokens.displayXlHeight;

  /// Shared displayXlTracking token.
  double get displayXlTracking => Tokens.displayXlTracking;

  /// Shared displayLMin token.
  double get displayLMin => Tokens.displayLMin;

  /// Shared displayLMax token.
  double get displayLMax => Tokens.displayLMax;

  /// Shared displayLViewportFactor token.
  double get displayLViewportFactor => Tokens.displayLViewportFactor;

  /// Shared displayLHeight token.
  double get displayLHeight => Tokens.displayLHeight;

  /// Shared displayLTracking token.
  double get displayLTracking => Tokens.displayLTracking;

  /// Shared displayMMin token.
  double get displayMMin => Tokens.displayMMin;

  /// Shared displayMMax token.
  double get displayMMax => Tokens.displayMMax;

  /// Shared displayMViewportFactor token.
  double get displayMViewportFactor => Tokens.displayMViewportFactor;

  /// Shared displayMHeight token.
  double get displayMHeight => Tokens.displayMHeight;

  /// Shared displayMTracking token.
  double get displayMTracking => Tokens.displayMTracking;

  /// Shared headingSize token.
  double get headingSize => Tokens.headingSize;

  /// Shared headingHeight token.
  double get headingHeight => Tokens.headingHeight;

  /// Shared bodyLSize token.
  double get bodyLSize => Tokens.bodyLSize;

  /// Shared bodySize token.
  double get bodySize => Tokens.bodySize;

  /// Shared bodySSize token.
  double get bodySSize => Tokens.bodySSize;

  /// Shared bodyHeight token.
  double get bodyHeight => Tokens.bodyHeight;

  /// Shared bodySHeight token.
  double get bodySHeight => Tokens.bodySHeight;

  /// Shared metaSize token.
  double get metaSize => Tokens.metaSize;

  /// Shared metaHeight token.
  double get metaHeight => Tokens.metaHeight;

  /// Shared telemetrySize token.
  double get telemetrySize => Tokens.telemetrySize;

  /// Shared telemetrySSize token.
  double get telemetrySSize => Tokens.telemetrySSize;

  /// Shared telemetryHeight token.
  double get telemetryHeight => Tokens.telemetryHeight;

  /// Shared arabicHeightIncrease token.
  double get arabicHeightIncrease => Tokens.arabicHeightIncrease;

  /// Shared bodyMeasureCharacters token.
  int get bodyMeasureCharacters => Tokens.bodyMeasureCharacters;

  /// Shared measureAdvanceRatio token.
  double get measureAdvanceRatio => Tokens.measureAdvanceRatio;

  /// Shared instant token.
  Duration get instant => Tokens.instant;

  /// Shared quick token.
  Duration get quick => Tokens.quick;

  /// Shared standard token.
  Duration get standard => Tokens.standard;

  /// Shared considered token.
  Duration get considered => Tokens.considered;

  /// Shared sequence token.
  Duration get sequence => Tokens.sequence;

  /// Shared reducedAcquisition token.
  Duration get reducedAcquisition => Tokens.reducedAcquisition;

  /// Shared emphasized token.
  Curve get emphasized => Tokens.emphasized;

  /// Shared exit token.
  Curve get exit => Tokens.exit;

  /// Shared linear token.
  Curve get linear => Tokens.linear;

  /// Shared spring token.
  SpringDescription get spring => Tokens.spring;

  /// Shared idleTraceFrequency token.
  double get idleTraceFrequency => Tokens.idleTraceFrequency;

  /// Shared traceFrameRate token.
  int get traceFrameRate => Tokens.traceFrameRate;

  /// Shared traceFrameBudget token.
  Duration get traceFrameBudget => Tokens.traceFrameBudget;

  /// Shared messageLifetime token.
  Duration get messageLifetime => Tokens.messageLifetime;

  /// Shared tooltipDelay token.
  Duration get tooltipDelay => Tokens.tooltipDelay;

  /// Shared touchTarget token.
  double get touchTarget => Tokens.touchTarget;

  /// Shared pointerTarget token.
  double get pointerTarget => Tokens.pointerTarget;

  /// Shared iconStroke token.
  double get iconStroke => Tokens.iconStroke;

  /// Shared iconGrid token.
  double get iconGrid => Tokens.iconGrid;

  /// Shared portraitAspectRatio token.
  double get portraitAspectRatio => Tokens.portraitAspectRatio;

  /// Shared skeletonTextFraction token.
  double get skeletonTextFraction => Tokens.skeletonTextFraction;

  /// Shared sweepOpacity token.
  double get sweepOpacity => Tokens.sweepOpacity;

  /// Shared sweepWidthFraction token.
  double get sweepWidthFraction => Tokens.sweepWidthFraction;

  /// Shared sweep token.
  Duration get sweep => Tokens.sweep;

  /// Shared sweepPause token.
  Duration get sweepPause => Tokens.sweepPause;

  /// Shared loaderWidth token.
  double get loaderWidth => Tokens.loaderWidth;

  /// Width of one `/console` stat tile.
  double get consoleTileWidth => Tokens.consoleTileWidth;

  /// Shared loaderHeight token.
  double get loaderHeight => Tokens.loaderHeight;

  /// Shared loaderFrequency token.
  double get loaderFrequency => Tokens.loaderFrequency;

  /// Shared placeholderImageWidth token.
  double get placeholderImageWidth => Tokens.placeholderImageWidth;

  /// Shared placeholderImageHeight token.
  double get placeholderImageHeight => Tokens.placeholderImageHeight;

  /// Shared placeholderImageByteLimit token.
  int get placeholderImageByteLimit => Tokens.placeholderImageByteLimit;

  /// Shared imageResolve token.
  Duration get imageResolve => Tokens.imageResolve;

  /// Shared carrierRestCycles token.
  double get carrierRestCycles => Tokens.carrierRestCycles;

  /// Shared carrierRestAmplitude token.
  double get carrierRestAmplitude => Tokens.carrierRestAmplitude;

  /// Shared carrierBurstWidth token.
  double get carrierBurstWidth => Tokens.carrierBurstWidth;

  /// Shared stationNodeMin token.
  int get stationNodeMin => Tokens.stationNodeMin;

  /// Shared stationNodeMax token.
  int get stationNodeMax => Tokens.stationNodeMax;

  /// Shared headerHeight token.
  double get headerHeight => Tokens.headerHeight;

  /// Shared railWidth token.
  double get railWidth => Tokens.railWidth;

  /// Shared footerHeight token.
  double get footerHeight => Tokens.footerHeight;

  /// Shared railProgressHeight token.
  double get railProgressHeight => Tokens.railProgressHeight;

  /// Shared heroRuleWidth token.
  double get heroRuleWidth => Tokens.heroRuleWidth;

  /// Shared heroRuleHeight token.
  double get heroRuleHeight => Tokens.heroRuleHeight;

  /// Shared grainTile token.
  double get grainTile => Tokens.grainTile;

  /// Shared grainSpecks token.
  int get grainSpecks => Tokens.grainSpecks;

  /// Shared acquisition token.
  Duration get acquisition => Tokens.acquisition;

  /// Shared stationNodeRadius token.
  double get stationNodeRadius => Tokens.stationNodeRadius;

  /// Shared stationNodeActiveRadius token.
  double get stationNodeActiveRadius => Tokens.stationNodeActiveRadius;

  /// Shared stationPulseRadius token.
  double get stationPulseRadius => Tokens.stationPulseRadius;

  /// Shared arcOpacity token.
  double get arcOpacity => Tokens.arcOpacity;

  /// Shared arcDraw token.
  Duration get arcDraw => Tokens.arcDraw;

  /// Shared graticuleStep token.
  double get graticuleStep => Tokens.graticuleStep;

  /// Shared mapAspectRatio token.
  double get mapAspectRatio => Tokens.mapAspectRatio;

  /// Shared traceScanningVelocity token.
  double get traceScanningVelocity => Tokens.traceScanningVelocity;

  /// Shared traceLockVelocity token.
  double get traceLockVelocity => Tokens.traceLockVelocity;

  /// Shared traceLockDistance token.
  double get traceLockDistance => Tokens.traceLockDistance;

  /// Shared traceRestDelay token.
  Duration get traceRestDelay => Tokens.traceRestDelay;

  /// Shared traceCycles token.
  double get traceCycles => Tokens.traceCycles;

  /// Shared traceAmplitude token.
  double get traceAmplitude => Tokens.traceAmplitude;

  /// Shared traceBurstWidth token.
  double get traceBurstWidth => Tokens.traceBurstWidth;

  /// Shared traceColumnFraction token.
  double get traceColumnFraction => Tokens.traceColumnFraction;

  /// Shared traceNoiseAmplitude token.
  double get traceNoiseAmplitude => Tokens.traceNoiseAmplitude;

  /// Shared markCurveSigmas token.
  double get markCurveSigmas => Tokens.markCurveSigmas;

  /// Shared markNativeSize token.
  double get markNativeSize => Tokens.markNativeSize;

  /// Shared markStroke token.
  double get markStroke => Tokens.markStroke;

  /// Shared markHeaderSize token.
  double get markHeaderSize => Tokens.markHeaderSize;

  /// Shared cartoucheHeroHeight token.
  double get cartoucheHeroHeight => Tokens.cartoucheHeroHeight;
}
