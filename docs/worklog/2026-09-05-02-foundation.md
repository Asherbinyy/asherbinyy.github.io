# 2026-09-05-02 — foundation

**Agent:** Codex / GPT-5
**Milestone:** 1
**Started from:** 2026-09-05-01-download-fonts.md

## Goal
Complete roadmap task 1.1 after creating the repository specification commit and phase branch. Establish the web-only Flutter foundation without implementing product screens beyond empty route placeholders.

## What changed
- Pinned FVM to Flutter 3.47.2 / Dart 3.13.2, committed the project specification and setup on `main`, then created `phase/1-ground-station` from it.
- Added the closed architecture dependency set, four subset font families, very_good_analysis, and the full feature-first directory structure.
- Implemented both documented palettes as a ThemeExtension, context accessors for palette and measurement tokens, both ThemeData artifacts, the full fluid type scale, Arabic line-height adjustment, and tabular figures on every text style.
- Centralized motion values and reduced-motion resolution, including the documented 200ms acquisition fallback.
- Added browser pointer-capability observation through conditional JS interop, capability-first platform resolution, and URL-path navigation without native code or user-agent detection.
- Added route placeholders for every Project Brief section 4 path. The static `/cv` and `/brief` implementations remain task 1.4 work.
- Added AppMessenger resolution with token-timed toast/snackbar behavior and complete pointer, touch, swipe, hover, focus, and semantic-close handling.
- Added unit and widget tests for platform resolution, messenger behavior, reduced motion, typography, route placeholders, theme artifacts, RTL direction, and direct route navigation.

## Files touched
- `.fvmrc` — created — pins Flutter 3.47.2 for the project.
- `.gitignore` — modified — applies the full documented generated-artifact and platform exclusions.
- `.vscode/settings.json` — confirmed — points the analyzer to the FVM SDK.
- `tool/fonts.sh` — created — subsets all ten supplied source TTF files with distinct Latin and Arabic OpenType feature retention.
- `assets/fonts/*-subset.ttf` — created — bundled subset font artifacts for four declared families.
- `docs/worklog/README.md` — created — retains the worklog directory.
- `pubspec.yaml` — modified — declares the exact architecture package set and local subset fonts.
- `analysis_options.yaml` — modified — enables very_good_analysis with generated-file exclusions.
- `build.yaml` — created — limits generators to pure-Dart provider files while the approved generator stack is incompatible with Flutter 3.47 library syntax.
- `lib/main.dart` — modified — bootstraps ProviderScope and root-relative browser navigation.
- `lib/app/` — created — root app, route enum/configuration, themes, typography, token extension, and l10n directory.
- `lib/core/motion/` — created — named duration, curve, and reduced-motion access points.
- `lib/core/platform/` — created — capability observation, platform resolution, browser navigation, secondary-surface policy, and transient messenger.
- `lib/core/widgets/placeholder_screen.dart` — created — empty themed foundation screen for all routes.
- `lib/core/{analytics,error,painting}/.gitkeep` — created — architecture directories reserved for later tasks.
- `lib/content/models/.gitkeep` and `lib/features/**/{data,domain,presentation}/.gitkeep` — created — full feature-first architecture directories.
- `test/unit/` and `test/widget/` — created — coverage for the foundation behaviors.
- `web/` and `.metadata` — created from the web-only Flutter scaffold — web shell and Flutter project metadata.

## Decisions made
- Used Flutter 3.47.2 / Dart 3.13.2 because the owner explicitly resolved the SDK pin to the already cached version.
- Kept both documented `alert` tokens as the sole genuine-error exception to the monochrome-plus-amber palette rule.
- Retained each Arabic font's full layout feature set (`--layout-features="*"`) so joining and marks remain correct; Latin faces retain only `kern,liga,tnum,onum`.
- Resolved platform behavior in this order: hover plus fine pointer, then coarse pointer, then the 1024px expanded breakpoint fallback.
- Installed only the closed package list from `03-ARCHITECTURE.md` section 4. `material_ui` resolves `cupertino_ui` transitively; there is no direct Cupertino dependency or import in app code.
- Kept generated files ignored. `build_runner` regenerates providers; `build.yaml` scopes generation because the prescribed Riverpod 2 / Freezed 2 analyzer range crashes against Flutter 3.47 syntax when it examines framework-dependent libraries. Pure-Dart providers generate successfully.
- Kept placeholder screens empty to avoid inventing English or Arabic user-facing content before localisation task 1.2.

## Tests
- Added: platform service, message controller, reduced motion, typography, AppMessenger widget behavior, route and theme placeholder tests.
- Modified: removed scaffold counter smoke test and replaced it with foundation route/theme coverage.
- Full suite: pass, 38 passing, 0 failing.
- Coverage delta: not measured.
- Manual: pass — opened the verified WASM build in Chrome; the browser loaded the shell, WASM module, manifest, and all declared local fonts.

## Verification run
```
fvm dart format --set-exit-if-changed .   pass (0 files changed)
fvm flutter analyze                        pass (no issues found)
fvm flutter test                           pass (38 passing, 0 failing)
fvm flutter build web --wasm               pass (built build/web)
```

## Known issues left open
- The ten prescribed subsets total 813,332 bytes, above the 480KB font budget. A trial that removed hinting still measured 595,672 bytes; weights, Unicode ranges, and Arabic shaping features were not reduced because the specification does not authorize that content or rendering tradeoff.
- `gh` is not installed and `git remote -v` returns no remote. The `asherbinyy.github.io` repository, pushes, and Pages workflow enablement could not be performed; no external repository state was changed.
- `fvm flutter build web --wasm` succeeds but reports that Material and Cupertino icon fonts are absent. App code uses no stock icon and intentionally leaves `uses-material-design` disabled because `01-DESIGN-SYSTEM.md` requires a custom icon set. Resolve this when custom icons are introduced.
- Static `/cv` and `/brief`, content, localisation copy, loading primitives, actual screens, CI, and deployment are intentionally deferred to their roadmap tasks.

## Next
Complete roadmap task 1.12 on `phase/1-ground-station`: add CI and deploy automation, then configure the GitHub remote and Pages when GitHub CLI or an equivalent authenticated workflow is available.
